namespace Core
{
    public enum LootStatus
    {
        CORPSE = 0,
        READY = 1,
        CLOSED = 2
    }

    public static class Loot
    {
        public const int LOOTFRAME_AUTOLOOT_DELAY = 300;
    }
}